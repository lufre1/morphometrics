# mito-morphometrics

Compute per-cell morphometrics from raw images and cell labels, optionally with mitochondria-per-cell summary.

## Install (conda)
```bash
conda env create -f env.yaml
conda activate mito-morphometrics